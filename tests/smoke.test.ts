import { describe, expect, it } from 'vitest'

// Placeholder smoke test to prove the Vitest + CI pipeline works end to end.
// Replace/expand once real ingestion/domain logic lands in Phase 2+.
describe('project scaffolding', () => {
  it('runs tests', () => {
    expect(1 + 1).toBe(2)
  })
})
